# ─── Full Stock — Variables de entorno ───────────────────────────────────────
# Copia este archivo como ".env" y rellena los valores reales.
# NUNCA subas el archivo .env a GitHub (ya está en .gitignore).

# MongoDB Atlas — Connection String
# Formato: mongodb+srv://<user>:<password>@<cluster>.mongodb.net/<dbname>?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://TU_USUARIO:TU_PASSWORD@TU_CLUSTER.mongodb.net/fullstock?retryWrites=true&w=majority

# JWT — Clave secreta para firmar tokens (mínimo 32 caracteres aleatorios)
JWT_SECRET=cambia-esto-por-una-cadena-aleatoria-larga-y-segura

# Cloudinary — Subida de imágenes
# Consíguelos en: https://console.cloudinary.com → API Keys
CLOUDINARY_CLOUD_NAME=tu-cloud-name
CLOUDINARY_API_KEY=tu-api-key
CLOUDINARY_API_SECRET=tu-api-secret

# CORS — Orígenes permitidos
# * → abierto (desarrollo)
# https://tu-dominio.com → un origen (producción)
# https://a.com,https://b.com → varios orígenes separados por coma
CORS_ORIGIN=*

# ─── Email (Nodemailer) — Recuperación de contraseña ─────────────────────────
# REQUERIDO para que se envíen correos. Si EMAIL_USER/EMAIL_PASS faltan, el
# sistema entra en "modo degradado" y solo imprime el enlace de reseteo en consola.
#
# Para Gmail: activa la verificación en 2 pasos y genera una "Contraseña de
# aplicación" (NO uses tu contraseña personal): https://myaccount.google.com/apppasswords
EMAIL_USER=tu-correo@gmail.com
EMAIL_PASS=tu-contraseña-de-aplicacion

# Opción A — Atajo de servicio (default: gmail). Úsalo si NO defines SMTP_HOST.
EMAIL_SERVICE=gmail

# Opción B — Host/puerto SMTP explícitos (sobrescribe EMAIL_SERVICE si se define).
# 465 = SSL directo · 587 = STARTTLS
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587

# Remitente visible (opcional)
EMAIL_FROM="Full Stock <tu-correo@gmail.com>"
